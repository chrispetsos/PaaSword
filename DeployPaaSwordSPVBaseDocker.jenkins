node {
    def paaswordSpvBaseImage

    stage('Clone') {
        checkout scm
    }

    stage('Test') {
    	sh 'mvn clean test'
    }

    stage('Build') {
    	sh 'mvn package -DskipTests=true'
        paaswordSpvBaseImage = docker.build("chrispetsos/paasword-spv-base", "-f ./BuildPaaSwordSPVBase.docker .")
    }

    stage('Deploy') {
        docker.withRegistry('https://registry.hub.docker.com', 'docker-hub-credentials') {
            paaswordSpvBaseImage.push("1.0.0")
        }
    }
}